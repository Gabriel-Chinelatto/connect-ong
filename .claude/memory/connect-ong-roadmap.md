---
name: connect-ong-roadmap
description: Full MASTER roadmap for Connect ONG to a professional finish — done blocks + all planned blocks with priority tiers
metadata: 
  node_type: memory
  type: project
  originSessionId: c6d1da92-9e74-4fa5-be2c-464ffcc31096
---

**PLANNING MODEL (user-set 2026-06-23):** keep ONE full master roadmap from start to "master finish", covering ALL originally-requested features (PDF + the user's GPT roadmap) + improvements I judge worthwhile, reflecting route changes (**desktop = ONG admin only, mobile = doador only, web = public/doador portal**). Work block by block. **As the presentation nears:** when the user says time is short, I COMPRESS the remaining blocks into ~2-3 to finish mobile+desktop cleanly. If all blocks finish WITH time left, the user says how much and I propose improvements. Keep until the user says we're close to the presentation. (Supersedes the earlier rolling re-plan rule.)

Priority tiers: **[CORE]** = needed for a strong professional demo; **[PLUS]** = valuable depth; **[STRETCH]** = nice-to-have, first to be cut/merged when time is short.

== DONE (Blocos 0-4, session 2026-06-22/23) ==
- 0 Fundação: DB creds externalized, Swagger, global exception handler, output DTOs.
- 1 Match backend: Necessidade + Interesse, full REST, accept/reject, no-duplicate rule.
- 2 Match telas: mobile doador (feed / tenho interesse / meus matches) + desktop ONG panel (publish, see interested, accept/reject).
- 3 Chat: Mensagem backend + polling chat UI both sides (remetente DOADOR/ONG), only on ACEITO match.
- 4 Dashboard de impacto: doador "Meu Impacto" stats + ONG panel stats header.

== PLANNED (forward) ==
**PHASE B — Profissionalização (July)**
- 5 [CORE] Limpeza & padronização: ✅ DONE. Mobile 55→0 lints (withOpacity→withValues x12 files, removed debug prints, mounted-guards, initialValue/activeThumbColor/scaleByDouble/super.key); desktop 4→0, deleted dead screens/empresa/* + empresa_service + cadastro_tipo, removed the "Empresa" concept (registration → CadastroOngScreen). Commits: mobile 5dc7187, desktop 82e4b66.
- 6 [CORE] ONG self-registration + account↔ONG link + finish persona split: ✅ DONE. Backend: Usuario.ongId column; POST /ongs/registro creates linked Ong profile + Usuario(tipo ONG, ongId) with BCrypt; login/cadastro return ongId (commit c0c7b07 on api master). Desktop: real ONG registration form (POST /ongs/registro), auth_service returns user map, login passes ongId/nome → PainelOngScreen uses ongId directly (email-match/selector now fallback only) (commit 3e3b525). Mobile = doador-only: removed Doador/ONG toggle (login always DOADOR → HomeDoadorScreen), deleted dead lib/receptor/*, root lib/login_page.dart, pedido.dart, pedido_card, tipo_usuario (commit 1ede197). Both apps + backend: zero lints, tested (register→login→ongId verified via curl).
- 7 [CORE] Design system & UX: ✅ DONE (foundation). Created `AppColors` palette in BOTH apps (single source, same brand green 0xFF0A8449); themes reference it; unified the divergent greens (mobile login was 0xFF2F8F46, desktop was 0xFF2E7D32 → both now 0xFF0A8449); applied Poppins typography on mobile too (matches desktop). Reusable components / responsiveness / feedback states already largely present. Deeper per-screen visual polish stays in Bloco 21. Commits: mobile 92e8015, desktop 87af12f.

- 8 [CORE] **Perfil & Central de Configurações** (user-requested; spec in [[connect-ong-profile-settings]]): ✅ DONE. Backend: Usuario profile fields (telefone/cidade/estado/bio/fotoUrl) + Preferencia entity (one/user, defaults) + PerfilController (GET/PUT perfil, PUT senha, GET/PUT preferencias) (api commit 24633c6). BOTH apps: ConfigController (ChangeNotifier) → MaterialApp via ListenableBuilder applies theme claro/escuro/auto + font size (textScaler) + dyslexia font (Lexend) + high contrast LIVE; loads prefs after login, clears on logout. Configurações screen (Aparência live + Notificações + Privacidade + Segurança/alterar senha) + Perfil screen (edit dados + avatar + stats). Mobile commits 7a50c6b, e29a0d4; desktop a2e4726. NOTE: "sessões ativas" still waits for JWT (Bloco 15); notification DELIVERY waits for Bloco 12 (prefs stored now). Full dark-mode coverage of every hardcoded-color screen is polish (Bloco 21).

**PHASE C — Confiança & Transparência (July)**
- 9 [PLUS] Verificação de ONG: ✅ DONE. Ong.verificada + cnpj; `PUT /ongs/{id}/verificar` (admin marks verified — no admin UI/auth yet, call via Swagger); OngResponseDTO + NecessidadeResponseDTO expose verified status. Mobile shows blue ✓ seal in the feed + "ONG Verificada" in ONG search; desktop registration collects CNPJ. Commits: api 6d0e083, mobile 4c9d95a, desktop 3da549b. (A proper admin approval screen could come later.)
- 10 [PLUS] Prestação de contas: ✅ DONE. Prestacao entity tied to an accepted Interesse (titulo, descricao, fotoUrl); `GET/POST /prestacoes?interesseId=`, rule "only on ACEITO match" (api commit d01e4a4). Desktop: "Prestar contas" button + dialog on accepted interests (mobile aabf5e7... wait that's mobile). Desktop commit d618f63. Mobile: doador sees "Prestação de contas" button on accepted matches → screen with relato + photo (commit aabf5e7). Tested end-to-end + pending-match guard.
- 11 [PLUS] Avaliações: ✅ DONE. Avaliacao entity (nota 1-5 + comentario, one per doador/ONG = upsert); `GET/POST /avaliacoes?ongId=`; recomputes Ong.notaMedia + totalAvaliacoes (denormalized, exposed in OngResponseDTO + feed/NecessidadeResponseDTO). Mobile: star-rating dialog on accepted matches (Conversar/Prestação/Avaliar actions) + average stars shown on feed cards. api commit abb2ce3, mobile 84c46ad. NOTE: ONG seeing its own rating in the desktop panel left as a small future add (needs a GET /ongs/{id} or fetch). **PHASE C (Confiança & Transparência, blocos 9-11) COMPLETE.**

**PHASE D — Tempo real & Engajamento (July/Aug)**
- 12 [PLUS] Notificações + (WebSocket?): **Notifications ✅ DONE** — Notificacao entity, events fire on interesse/aceite/mensagem/prestação, **respects Bloco 8 prefs** (notifMatch etc. gate creation), bell+badge+screen on both apps, mark-read. api commit e2015b2; mobile 3025430; desktop 9625846. **WebSocket upgrade: SKIPPED by user decision (2026-06-24)** — keep the reliable 2s-polling chat; not worth the risk for FECITEC. Bloco 12 considered DONE. (Could revisit at the very end only if lots of time remains.)
- 13 [PLUS] Feed melhorado + **Feed Inteligente**: ✅ DONE. Backend exposes ongCidade in NecessidadeResponseDTO (api d7db3b8). Mobile feed: search bar (titulo/ONG/categoria), filter chips (Urgentes + categorias derived from data), smart ordering (urgentes first, then ONGs in the doador's city — city from perfil). Mobile commit 983a5f4. (Maps idea #15 stays dropped; city-based ordering covers it.)

**PHASE E — Pagamentos (Aug)**
- 14 [STRETCH] Doação financeira (PIX simulado): ✅ DONE. DoacaoFinanceira entity (ongId/doadorId/valor/codigoPix/status CONFIRMADO); `POST/GET /doacoes-financeiras`; generates a fake "copia e cola" PIX code; notifies the ONG. Mobile: DoarPixScreen (valor + atalhos → comprovante with copy button), wired to the "Doar via PIX" button in buscar_receptor. No real gateway (TCC). api commit a01b680, mobile d58f17e. NOTE: a "doações recebidas" list on the desktop ONG panel is a small future add (ONG already gets a notification).

**PHASE F — Segurança & Conformidade (Aug)**
- 15 [PLUS] JWT + refresh token auth (BCrypt already in place); LGPD (consent, privacy policy, terms): ✅ DONE. JWT via jjwt 0.12.6 (NO Spring Security — API stays open by design; enforcement OFF until near final delivery). JwtService (gerarAccessToken 1h / gerarRefreshToken 7d / validar); login+cadastro+ONG-registro now return accessToken/refreshToken in UsuarioResponseDTO; AuthController POST /auth/refresh + GET /auth/me (Bearer). Tested end-to-end (positive + 401 on tampered token). LGPD: DocumentosLegaisScreen (Política de Privacidade + Termos de Uso) in BOTH apps; consent checkbox w/ tappable links blocks ONG cadastro (desktop) until accepted; links added to Configurações of both apps. Completes "sessões ativas / logout global" from Bloco 8.
- 16 [STRETCH] Audit logs + hardened server-side validation: ✅ DONE (backend-only; admin-facing, demonstrável via REST/Swagger). AUDIT: AuditLog entity (acao/usuarioId/descricao/ip/criadoEm) + AuditService.registrar (best-effort, nunca quebra o fluxo; captura IP via RequestContextHolder) + GET /audit-logs (limite + filtro usuarioId). Hooks: CADASTRO_USUARIO, CADASTRO_ONG, LOGIN_SUCESSO, LOGIN_FALHA (senha errada c/ id + email inexistente s/ id), DOACAO_FINANCEIRA. NUNCA grava senha/token. VALIDATION: jakarta @Valid em todos os DTOs de escrita (Ong/Necessidade/Interesse/Mensagem/Avaliacao/Prestacao/DoacaoFinanceira) — @NotBlank/@NotNull/@Email/@Size/@Positive/@Min-Max/@DecimalMin-Max/@Pattern; GlobalExceptionHandler reforçado (MethodArgumentNotValid→400 c/ campos, ConstraintViolation, HttpMessageNotReadable JSON malformado→400, TypeMismatch→400, genérico→500 SEM vazar detalhe). Login/cadastro seguem com Usuario entity + validação manual no service (login só manda email+senha). api commit (master).

**PHASE G — Escalabilidade & DevOps (Aug)**
- 17 [PLUS] DB migrations + indexes + normalization review (move off ddl-auto=update): ✅ DONE. **Liquibase em vez de Flyway** — Flyway Community gateia MySQL 5.6 (confirmado: o banco é 5.6.44) para a Teams Edition paga, em TODAS as versões testadas (8.5.13 e 7.15.0); Liquibase é gratuito e suporta 5.6. ddl-auto agora = **validate** (testado: sobe limpo, entidades batem com schema). Changelog db.changelog-master.yaml com 9 índices idempotentes (preCondition not indexExists/MARK_RAN) em colunas findBy não indexadas (necessidade.status, notificacao.usuario_id, doacao_financeira.doador_id+ong_id, avaliacao.ong_id+doador_id, audit_log.usuario_id+acao, usuario.ong_id); FKs já indexadas pelo Hibernate ficaram de fora. Idempotência verificada (restart = Run:0/Previously run:9). docs/NORMALIZACAO.md documenta 3FN + snapshots intencionais (doacao_financeira/avaliacao guardam nome = comprovante histórico) + pendências (tabelas legadas doacao/projeto, baseline completo via generateChangeLog). api commit ea7ce42 (master).
- 18 [STRETCH] Docker + CI/CD (GitHub Actions) + automated tests (backend unit + Flutter widget): ✅ DONE. DOCKER: Dockerfile multi-stage (maven build → JRE-17 runtime, jar=connectong-api-0.0.1-SNAPSHOT.jar) + .dockerignore + docker-compose.yml (API→MySQL remoto, senha via env). NÃO validado localmente (Docker não instalado na máquina) — padrão correto. TESTS: backend 8 testes verdes (JwtServiceTest 3 + ValidacaoDtoTest 4 + contextLoads com H2 em memória; test/resources/application.properties usa H2 MODE=MySQL, ddl-auto=create-drop, liquibase off — CI não toca o banco da escola); mobile+desktop 2 testes cada (Ong.fromJson); desktop widget_test padrão (contador) substituído. CI: 3 workflows .github/workflows — backend-ci.yml (JDK17 temurin + chmod mvnw + mvnw verify, working-dir na subpasta com espaços), flutter-ci.yml em cada app (subosito/flutter-action stable + pub get + analyze + test). flutter analyze 100% limpo nos 2 apps. Rodam no push/PR (ainda não pushados). Microsserviços = SKIP (decisão registrada). commits: api 000df43 (master), mobile 765172d, desktop 656dba6 (main).

**PHASE H — Web & Entrega (Aug)**
- 19 [CORE] Web build published + **rich institutional portal** (absorbs idea #12, "startup-like"): ✅ DONE. Web = build Flutter-web do app do doador com o **portal institucional** como entrada (kIsWeb → PortalInstitucionalScreen; mobile segue no login; botão "Sobre o Projeto" do login abre o portal). Portal (lib/web/portal_institucional_screen.dart, responsivo): hero, **estatísticas públicas ao vivo**, sobre (missão/visão/valores), como funciona (3 passos), ODS 1/2/10/17, equipe (4 integrantes c/ fotos), FAQ (ExpansionTile), transparência (links p/ DocumentosLegaisScreen LGPD), rodapé. Backend: **GET /publico/estatisticas** (sem login) via PublicoController + EstatisticasPublicasDTO (totais ONGs/doadores/necessidades/matches ACEITO/doações fin+valor somado/prestações); repos: countByTipo, countByStatus, SUM(valor). `flutter build web --release` OK (Built build/web). NÃO hospedado ainda (deploy GitHub Pages/Netlify é passo opcional). commits: api 8854198 (master), mobile 38654e9 (main).
- 20 [CORE] Dados de demonstração caprichados + **Modo Feira** (absorbs idea #13): ✅ DONE. Backend DemoService.carregar() **idempotente** (marcador = email demo.larviva; se existe → "ja_carregado" sem duplicar) cria 4 ONGs (c/ contas) + 3 doadores + 9 necessidades + 3 interesses (2 ACEITO) + 2 mensagens de chat + 1 prestação + 2 avaliações (atualiza nota_media) + 2 doações PIX; senha padrão de todas as contas demo = "demo123"; sem emoji em strings salvas. **POST /demo/seed** (DemoController). Botão "Carregar dados demonstrativos" no **desktop** (Configurações → seção "Modo Feira") via DemoService(dart), com dialog mostrando contas de exemplo. **ROTEIRO_DEMO.md** (roteiro de apresentação FECITEC) no repo mobile. Testado: 1ª chamada carregou (20 ONGs/15 users/17 necessidades), 2ª = ja_carregado. commits: api (master), desktop (main).
- 21 [CORE] MASTER FINISH: ✅ DONE (mobile na sessão 2026-07-02 = remodelação completa, ver [[connect-ong-remodel-mobile]]; desktop+portal web na sessão 2026-07-03 = varredura completa de dark mode/Colors hardcoded, painel com cabeçalho identidade (avatar+selo+4 stats), snackbars no tema, portal institucional alinhado ao design system). Pendente humano: A0 poster review + ensaio (ROTEIRO_DEMO.md existe).

**PHASE I — Complementos: Vida & Engajamento (added 2026-06-24 from user's 16-idea list; goal = the professors' "ter vida" feedback).** These run after the core line (or interleaved by priority); compress-at-deadline rule applies.
- 22 [CORE] **Sistema de Campanhas** (idea #1, the strongest): ✅ DONE. Reaproveitou a entidade Projeto → **Campanha** via **migration Liquibase real** (renomeia tabela projeto→campanha + addColumn valor_arrecadado/categoria/data_inicio/data_fim/encerrada/destaque; FIX: Liquibase cria boolean como TINYINT mas Hibernate validate espera BIT → changeset extra ALTER MODIFY BIT). Campos meta/arrecadado/progresso%/datas/encerrada/destaque. API /campanhas: GET (filtra ongId, abertas=true), GET /destaques, POST criar (@Valid), POST /{id}/contribuir (incrementa, **auto-encerra ao bater meta**, notifica ONG), PUT /{id}/encerrar; filtra campanhas órfãs (sem ONG) das respostas. Removeu Projeto* (código morto). Seed: 3 campanhas demo idempotentes (ensureCampanha por título). MOBILE: CampanhasScreen (lista abertas + barra progresso + dialog Contribuir com valores rápidos) + card no home. DESKTOP: 3ª aba "Campanhas" no painel (lista c/ progresso + Nova campanha/_FormCampanha + Encerrar). Testado backend (30%→100% auto-encerra) + analyze/test verdes. commits: api 5ac56f0, mobile 3164081, desktop 5c05021.
- 23 [PLUS] **Timeline de Atividades** (idea #2; #11 personal-history merged in): ✅ DONE. Feed global de atividades recentes da plataforma reaproveitando os hooks de eventos. BACKEND: entidade Atividade (tipo/descricao/ongId/ongNome/dataCriacao) + AtividadeService.registrar **best-effort** (padrão AuditService, nunca quebra o fluxo) + GET /atividades (feed global; filtra ongId; limit padrão 30, max 100) + AtividadeResponseDTO; migration Liquibase bloco23 (tabela atividade, data_criacao=datetime p/ casar c/ LocalDateTime do Hibernate — **sem boolean = sem gotcha BIT/TINYINT** do bloco 22 — + 2 índices idempotentes); hooks registrar() em **8 call-sites** (necessidade publicada, interesse demonstrado [doador ANÔNIMO no feed público], match aceito, prestação, campanha criada, campanha bateu meta, doação PIX [SEM valor/doador por privacidade], avaliação); DemoService semeia 7 atividades demo idempotentes (count==0). Validado no MySQL 5.6 real (Liquibase aplicou, validate passou, GET 200 c/ 7 eventos); mvnw test 10/10. MOBILE: model+service+TimelineAtividadesScreen (card por evento c/ ícone por tipo + tempo relativo "agora/há X min/h/d" + pull-to-refresh) + HomeCard "Atividades" no home. DESKTOP: model+service + **4ª aba "Atividades"** no painel (feed global, carga best-effort). analyze "No issues" + testes verdes nos 2 apps. commits: api 08c75cd (master), mobile e3e59d0, desktop 90035e9 (main) — **pushados**. **Próximo = Bloco 24 (Perfil Público da ONG).**
- 24 [PLUS] **Perfil Público da ONG** (idea #3): ✅ DONE. BACKEND: **GET /ongs/{id}/perfil-publico** → PerfilPublicoOngDTO (404 se não existe), agrega dados+selo+notaMédia + listas de necessidades/campanhas/avaliações/prestações + contadores, reusando os DTOs/services existentes; PrestacaoRepository.findByInteresseNecessidadeOngIdOrderByDataCriacaoDesc (prestacao→interesse→necessidade→ong); ONGService.perfilPublico. Validado MySQL 5.6 (200 c/ perfil completo da Lar Viva + 404), mvnw test 10/10. MOBILE: PerfilPublicoOngScreen (avatar de inicial — **logo/história/missão NÃO viraram colunas** p/ evitar migração+forms vazios, fica p/ enriquecimento/Bloco 21 — selo, nota em estrelas, Sobre, Contato, Campanhas c/ progresso, Necessidades, Prestações, Avaliações; **Compartilhar = copia resumo p/ clipboard**, sem add share_plus); aberto pelo botão "Ver perfil" nos cards de Buscar Receptor. DESKTOP: mesma tela como **pré-visualização** ("Ver meu perfil público" na AppBar do painel). analyze "No issues" + testes verdes nos 2 apps. commits: api 79f6f06 (master), mobile 2498d46, desktop 77d322d (main) — **pushados**. **Próximo = Bloco 25 (Mural de Impacto).**
- 25 [PLUS] **Mural de Impacto** (idea #8): ✅ DONE. Vitrine pública dos números coletivos da plataforma, **reaproveitando GET /publico/estatisticas** (sem backend novo). "Pessoas alcançadas" = estimativa transparente (conexões+doações), calculada no cliente. **Primeira feature com ilustração SVG** (diretriz do usuário [[preferencia-graficos]]): `assets/images/impacto_hero.svg` autorado original (ripple+coração+pessoas, cores da marca) via **flutter_svg ^2.0.10**; contadores animados (TweenAnimationBuilder). MOBILE: MuralImpactoScreen (hero SVG + destaque pessoas + grade de 6 stats + total PIX) + HomeCard "Nosso Impacto". DESKTOP: EstatisticaService criado + MuralImpactoScreen + botão "Mural de Impacto" na AppBar do painel. analyze "No issues" + testes verdes nos 2 apps. commits: mobile 9fac5fc, desktop f0084e8 (main) — pushados. (Backend inalterado.) **Próximo = Bloco 26 (Ranking de Transparência).** **VERIFICAÇÃO VISUAL FEITA** (técnica reutilizável: harness temporário `lib/main_screenshots.dart` que escolhe a tela pelo `#fragment` → `flutter build web --release -t` → servidor Dart estático mínimo só com dart:io → **Chrome headless `--virtual-time-budget` print** → ler PNG; debug-web NÃO renderiza no headless, precisa RELEASE). Telas conferidas com dados reais: Mural, Timeline, Perfil Público, Buscar/Ver-perfil — todas OK. **BUG achado e corrigido:** ilustração SVG do Mural ficava invisível (verde sobre hero verde) → agora vai num **cartão branco** (mobile 5d59965, desktop e8df0ad). Contadores animam (print pega no meio = normal). Possível leve aperto na borda direita = artefato da largura do print, não confirmado como bug do app.
- 26 [STRETCH] **Ranking de Transparência** (idea #4): ✅ DONE. Score 0-100 de sinais públicos existentes (SEM nova tabela): verificada(25)+nota(25)+prestações(25)+campanhas concluídas(25); níveis OURO≥75/PRATA≥45/BRONZE. TransparenciaService + GET /ongs/{id}/transparencia + GET /publico/ranking; perfil-publico passou a incluir transparenciaScore+nivelTransparencia. Frontends (medalha bronze/prata/ouro, cor por nível, Icons.workspace_premium): mobile = badge no perfil + RankingTransparenciaScreen + HomeCard "Ranking"; desktop = badge no perfil-preview + tela ranking + botão na AppBar. Validado (Lar Viva 60/PRATA, ranking ordenado), mvnw test 10/10, analyze limpo. commits: api 1d7daa9, mobile 027c4c9, desktop 765f77f.
- 27 [STRETCH] **Conquistas / gamificação** (idea #5): ✅ DONE. Conquistas derivadas de contagens (SEM nova tabela), lista completa (conquistadas+bloqueadas). GET /conquistas/doador/{usuarioId} (PRIMEIRA_DOACAO/RECORRENTE≥3/CINCO/DEZ; contribuições=doaçõesPIX+matches aceitos) + /conquistas/ong/{ongId} (VERIFICADA/PRIMEIRA_CAMPANHA/PRIMEIRA_PRESTACAO/PRIMEIRA_AVALIACAO/CAMPANHA_CONCLUIDA). Frontends: mobile ConquistasScreen (id via SessionService) + HomeCard; desktop ConquistasScreen(ongId) + botão painel; badge colorido/cadeado + contador. mvnw test 10/10. commits: api 3ee04a1, mobile f542a64, desktop 2fc7578. (Durante este bloco a instância hot-reload travou → reiniciei a API limpa; lição: ao adicionar @RestController novo, se /endpoint der 404 persistente, REINICIAR a API em vez de confiar no DevTools.)
- 28 [STRETCH] **Favoritos** (idea #6): ✅ DONE. Tabela `favorito` (migration Liquibase bloco28: datetime, sem boolean; +índices usuario e tipo/alvo) — validada no MySQL 5.6. Favorito entity+repo+service+controller+FavoritoResponseDTO (resolve nome do alvo). GET /favoritos?usuarioId, GET /favoritos/ids?usuarioId&tipo (p/ marcar coração), POST (idempotente), DELETE. Hook: ao publicar necessidade, notifica (best-effort) quem favoritou a ONG. Frontend SÓ mobile (doador-only): FavoritosScreen + HomeCard + coração toggle nos cards de ONG (Buscar Receptor) e campanha. CRUD validado por curl, mvnw test 10/10. commits: api 87a0286, mobile 723997f.
- 29 [STRETCH] **Denúncia** (idea #10): ✅ DONE. Tabela `denuncia` (migration bloco29: datetime, +índice status) — validada MySQL 5.6. Denuncia entity+repo+service+controller + DenunciaRequestDTO(@Valid)/ResponseDTO. POST /denuncias (status ABERTA, registra AuditLog DENUNCIA), GET /denuncias?status (admin/Swagger), PUT /{id}/resolver. Frontend SÓ mobile: botão "Denunciar" (flag) na AppBar do Perfil Público → dialog (motivo dropdown + detalhes), denuncianteId via SessionService (anônimo se deslogado). Validado curl (201, 400 sem motivo, resolver). commits: api 13cffa3, mobile 5002e6d. (Sem UI de admin p/ moderar — consulta via Swagger; mesma pegada do selo de verificação.)
- 30 [STRETCH] **Relatórios PDF** (idea #14): ✅ DONE. Frontend-only (sem backend), pacotes **pdf (3.12) + printing (5.14)** nos 2 apps. MOBILE: RelatorioPdfService.historicoDoacoes → PDF do histórico (model Doacao legado só tem nome/descricao/quantidade/categoria/tipo — sem data/status) + botão "Exportar PDF" em Minhas Doações (Printing.layoutPdf). DESKTOP: RelatorioPdfService.relatorioOng(PerfilPublicoOng) → MultiPage A4 (cabeçalho nome/cidade/selo/transparência, stats, sobre, tabela de campanhas, listas necessidades/prestações) + botão "Relatório PDF" no painel. analyze "No issues" + testes verdes nos 2 apps. NOTA: rodar o app desktop com printing exige Windows **Developer Mode** (symlinks de plugin) — não afeta analyze/test. commits: mobile 076f15c, desktop cf580e5. **TODOS OS BLOCOS DE FUNÇÃO (0-20, 22-30) CONCLUÍDOS. Resta só o Bloco 21 (polimento visual MASTER FINISH) + recapitular [[connect-ong-deferred]].**

== PÓS-ROADMAP — melhorias funcionais adicionadas na sessão 2026-07-02 (além dos blocos 0-30) ==
Rodada guiada pelo usuário APÓS os blocos de função, antes do Bloco 21. Todas verificadas ao vivo (H2) + testes e **pushadas** nos 3 repos.
- **2 rodadas de qualidade+segurança** e **ROLE_ADMIN** e **soft-delete de contas** → detalhados em [[connect-ong-deferred]] (com commits). Backend passou p/ **40 testes**.
- **Chat estilo WhatsApp** (NOVO; expande o Bloco 3, que era só polling simples): (a) **recibo de leitura "visto"** — coluna `mensagem.data_leitura`, marca lido ao abrir o chat, cliente mostra 1/2 checks; (b) **presença online / "visto por último"** — coluna `usuario.ultimo_visto`, batimento no poll do status; (c) **"digitando…"** — estado efêmero em memória (`DigitandoRegistry`), heartbeat `POST /mensagens/digitando`; (d) **reações com emoji** — tabela `reacao` guardando o CÓDIGO (LIKE/LOVE/LAUGH/WOW/SAD/PRAY, **utf8-safe**: nunca o emoji cru por causa do MySQL 5.6), toggle 1-por-participante, long-press na bolha. Endpoints novos: `GET /mensagens/status`, `POST /mensagens/digitando`, `POST /mensagens/{id}/reacao`. Commits: backend `84350ea`(presença)+`bc4c1ba`(reações), mobile `eca9b66`+`c848c9a`, desktop `5d02b06`+`b2f701c`. Seed enriquecido mostra visto+reações no chat demo (`9f92277`).
- **Modo Feira (credenciais no login)** e **botão "Excluir minha conta"** e **sincronizar.bat/skill /sincronizar** (1 clique, anti-regressão, auto-gera settings.local.json) → detalhados em [[connect-ong-notebook-fecitec]].
- **Banco da escola populado**: migrações (presença/reações/soft-delete) aplicadas via Liquibase no startup + `/demo/seed`. Contas demo senha `demo123`; admin dev provisionado. **Rotacionar essas senhas p/ uso real** (ver [[connect-ong-deferred]]).

== SESSÃO 2026-07-03 — "faça tudo o que falta" (fechamento do roadmap) ==
Executada com 4 subagentes (3 paralelos por repo + 1 de verificação visual). Tudo commitado e **pushado** nos 3 repos.
- **"Esqueci a senha" ponta a ponta** (item do backlog): backend `POST /auth/esqueci-senha` (sempre 200 genérico anti-enumeração; com `app.demo.enabled=true` devolve `codigoDemo` — simulação de e-mail p/ feira, sem SMTP) + `POST /auth/redefinir-senha` ({email,codigo,novaSenha}, 400 genérico); código 6 dígitos SecureRandom, 15 min, tabela `senha_reset` via Liquibase (datetime, sem boolean). UI em 2 passos no mobile (`pages/esqueci_senha_page.dart`) e desktop (`screens/auth/esqueci_senha_screen.dart`), card "Modo demonstração" mostra o codigoDemo. Integração E2E provada ao vivo (H2): 11/11 passos.
- **Rate limiting** (`RateLimitService` in-memory, sem dependência): login 5 falhas/15min→429 por email+IP; esqueci-senha e cadastros públicos 5/15min por IP; 5 códigos errados no redefinir → 400 sempre. Configurável `app.ratelimit.*`; DESLIGADO no properties de teste. Mitiga enumeração de e-mail (mensagem "Email já cadastrado" mantida pela UX).
- **Bug B2 fechado**: /favoritos e /campanhas/contribuir com DTOs tipados → 400 em vez de 500. Backend agora com **62 testes**.
- **Minhas Doações (mobile) reestruturada**: 3 seções — Doações via PIX (`GET /doacoes-financeiras?doadorId=`, total no cabeçalho), Itens doados (matches ACEITOS), ofertas (CRUD original via `/doacoes/minhas`); novo `models/doacao_financeira.dart`; PDF exporta dados reais (data/ONG/valor+total).
- **LGPD no cadastro mobile**: checkbox obrigatório com links p/ DocumentosLegaisScreen.
- **Desktop — aba "Doações"** no painel (5ª aba; `GET /doacoes-financeiras?ongId=`, codigoPix omitido, total em card gradiente) + painel com `abaInicial` param.
- **Bloco 21 desktop/portal concluído** (ver linha do bloco acima).
- **Verificação visual por screenshots** (13 telas, 2 temas): tudo OK; 2 fixes (pluralização "1 item doado" mobile `7e0166c`; `abaInicial` desktop `5c3cda8`). **Gotcha novo da técnica**: Chrome headless no Windows tem largura mínima de janela ~500 — p/ viewport mobile exato (430px) sirva um `frame.html` com `<iframe>` de 430x932 no MESMO servidor HTTP (file:// não funciona); desktop: janela 1416x995 → viewport 1400x900.
- Commits principais: backend `381eb0e`+`d54b034` (master); mobile `d86239b`,`aecdac4`,`8c403e8`,`087c3a5`,`7e0166c` (main); desktop `40fb918`,`469a09f`,`a4ef1a8`,`a424a84`,`5c3cda8` (main).

ABSORBED into existing blocks (not new blocks): #7 Central de Notificações = already DONE (Bloco 12); #9 Feed Inteligente → Bloco 13; #12 Página Institucional → Bloco 19; #13 Modo Feira → Bloco 20; #16 app-real prep → Bloco 15 (JWT) + image-upload when needed + tech-guidelines (not a standalone block). DROPPED: #15 Mapa de ONGs (Maps-on-Flutter-web risk/cost; replaced by city search in Bloco 13).

Also-optional (from original GPT roadmap, only if huge time remains): recommendation engine, chatbot/AI moderation. See [[connect-ong-vision]], [[connect-ong-tech-guidelines]], [[connect-ong-milestones]], [[connect-ong-delivery-rules]], [[connect-ong-banca-feedback]].
