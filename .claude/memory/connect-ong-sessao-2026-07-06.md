---
name: connect-ong-sessao-2026-07-06
description: "CONCLUÍDA — 2ª rodada de feedback (13 tópicos): backend bloqueio+privacidade, mobile detalhe/interesse/impacto, desktop bloqueio, mobile2 UF-cidade/config/contraste/Maps/chat-bloqueado. Verificada visualmente e no ar."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5efb506b-4e50-4863-bb56-8bc40ab1a110
---

Sessão de 2026-07-06 (PC principal). Usuário viu os 2 apps rodando (builds release nas portas 5100/5100 + API real 8080/MySQL escola) e mandou a **2ª rodada de feedback: 13 tópicos**. Autonomia TOTAL confirmada: **não parar entre ondas, commit+push por checkpoint, sem pedir autorização** (registrado em [[git-workflow-preferences]]). Ver também [[connect-ong-roadmap]] (histórico) e [[connect-ong-deferred]].

## Os 13 tópicos do usuário (com as decisões tomadas)
1. 🔥 do card #1 invisível (chama laranja sobre chip laranja) → chama BRANCA + dias no chip.
2. Editar perfil: **Estado ANTES de Cidade**, ambos por SELEÇÃO (UF dropdown alfabético → autocomplete de cidades daquela UF) — asset OFFLINE do IBGE (`assets/dados/municipios_por_uf.json`), mais seguro que API na rede da escola. Aplicar em editar perfil + cadastro doador (mobile) e cadastro/editar ONG (desktop).
3. Clicar em necessidade urgente/card do feed → **tela de DETALHE da necessidade** (nova; capa, ONG clicável, descrição, cidade, "postado há X", Tenho interesse). ONG em destaque → abre o perfil direto.
4. Feed: se já demonstrei interesse → botão vira "Interesse demonstrado" (desabilitado) e a necessidade vai pro FIM da lista.
5. Privacidade REAL: toggles "exibir telefone/e-mail" passam a valer no perfil público (backend omite); Configurações só aplicam com botão **"Salvar configurações"** (aparece quando algo muda).
6. Configurações mais bonitas + **alto contraste** e **navegação simplificada** de verdade (alto contraste = fundos puros/bordas/texto pesado; simplificada = desliga carrossel automático/animações/confete + alvos de toque maiores).
7. Impacto: 4 destinos DISTINTOS — Matches realizados→Ativas; ONGs apoiadas→Explorar; Interesses enviados→Aguardando; **stat "Aguardando resposta" SUBSTITUÍDO por "Doações PIX"**→Minhas Doações (decisão minha, usuário delegou).
8. "Abrir no Maps" falhou na web → fix: `launchUrl` direto sem `canLaunchUrl`, fallback copia o link. Aplicar em todo url_launcher dos 2 apps.
9. Cadastrar doação (oferta): cabeçalho claro "Nova doação — preencha os dados para disponibilizar sua doação a qualquer ONG que precise" (não é direcionada).
10. "Concluída só quando prestar contas?" → DECISÃO (opinião dada ao usuário): MANTER o modelo 2 etapas (CONCLUIDO no "doação recebida" + prazo 10 dias), senão a contagem regressiva/penalidade perdem sentido; na UI da aba Concluídas, chip "⏳ Aguardando prestação de contas" vs "✅ Prestação publicada". Fácil de trocar depois se ele preferir.
11. Distância/rota até a ONG → botão **"Como chegar"** abrindo Google Maps DIRECTIONS (https://www.google.com/maps/dir/?api=1&destination=<endereço ONG>; origem vazia = localização atual do usuário). Rota in-app exigiria API paga — descartado.
12. Explorar: ONG do card clicável → perfil; mostrar "há X dias" (backend expõe `dataCriacao` no NecessidadeResponseDTO).
13. **Bloqueio estilo WhatsApp**: chat com avatar/nome clicável → perfil; ONG bloqueia doador pelo perfil dele; doador bloqueado: ONG some do feed/busca/campanhas, perfil dela vira {bloqueado:true}, mensagens → 403. Desbloqueio na lista "Doadores bloqueados" das Configurações do desktop.

## Contratos fixados para o backend (frontends construídos contra eles)
- `POST /bloqueios` {doadorId} (só ONG, ongId do token, idempotente) → 200 {mensagem}; `DELETE /bloqueios/{doadorId}`; `GET /bloqueios` → [{doadorId, doadorNome, criadoEm}].
- Enforcement: POST /mensagens → 403 genérico p/ bloqueado; GET /necessidades, /campanhas e /ongs filtram ONGs bloqueadoras quando o requisitante é doador logado (anônimo não filtra); GET /ongs/{id}/perfil-publico p/ doador bloqueado → {id, nome, bloqueado:true}; GET /interesses?doadorId= inclui `bloqueadoPelaOng:true` nos matches afetados.
- Privacidade: perfil-publico da ONG omite email/telefone conforme campos da entidade Preferencia (nomes reais = descobrir na entidade; agente reporta).
- `NecessidadeResponseDTO.dataCriacao` (datetime; null nas antigas se coluna nova).

## Plano em 3 ondas (estado ao gravar esta memória)
- **ONDA 1 (RODANDO agora, 3 agentes paralelos lançados ~08:40)**:
  (a) BACKEND: bloqueio+privacidade+dataCriacao, testes MockMvc, live H2 na porta 8099 (NUNCA 8080 — tem demo rodando), commit/push master. Base: 82 testes.
  (b) MOBILE 1 (repo connect-ong): tópicos 1, 3, 4, 7, 9, 10(chip), 12 — tudo UI local, degrada sem backend novo. Base: 50 testes.
  (c) DESKTOP: tópico 13 (UI bloqueio: botão no PerfilPublicoDoadorScreen + lista nas Configurações + chat header clicável), 8 (fix Maps), 5 (Salvar configurações), 2 (UF/cidades no cadastro/editar ONG + geração do asset IBGE). Base: 13 testes.
- **ONDA 2 (disparar quando Mobile 1 terminar — mesmo repo)**: MOBILE 2: tópico 2 (UF/cidades no editar perfil + cadastro; copiar/gerar mesmo asset IBGE), 6 (configurações bonitas + alto contraste/nav simplificada REAIS via ConfigController), 5 (botão Salvar no mobile), 8 (fix Maps mobile), 11 (Como chegar), 13 lado doador (chat header → perfil ONG; tratar 403 de mensagem; perfil {bloqueado:true} → tela "Você não pode ver este perfil"; feed já vem filtrado do servidor).
- **ONDA 3**: E2E do bloqueio ao vivo (H2), verificação visual por screenshots (harness JÁ VERSIONADO: `lib/main_screenshots.dart` + `tool/screenshots/`; gotchas: build RELEASE, iframe 430px p/ viewport mobile, `--virtual-time-budget=6000` p/ telas com timer, 20000 p/ painel), rebuild dos 2 apps + servidores 5100/5200 (script `scratchpad/servidor.dart` — recriar de qualquer pasta build/web se sumir), memórias + relatório final.

## ✅ ONDA 3 CONCLUÍDA (2026-07-06 tarde, sessão Opus) — RODADA 2 FECHADA
O Opus retomou do handoff e fechou tudo:
- **Integração Mobile 2 revisada e confirmada** (as 6 tarefas cabeadas corretamente): SeletorUfCidade em editar-perfil E cadastro; ConfiguracoesScreen redesenhada (cards+descrições) com Salvar/Descartar via `mapEquals(_p.toJson(), _original.toJson())`; alto contraste + navegação simplificada REAIS (AppTheme._build muda bg/surface/texto/primária/bordas; carrossel para o Timer e confete/animações desligam quando navegacaoSimplificada); fix Maps (`Uri.https('www.google.com','/maps/dir/',...)` sem canLaunchUrl); botão "Como chegar" (perfil_publico_ong_screen linha ~699); chat 403→`_bloqueado`+"Você não pode enviar mensagens para esta ONG", perfil `{bloqueado:true}`→"Você não pode ver este perfil". Único conserto do orquestrador: import faltante `mapEquals` (flutter/foundation) em configuracoes_screen.dart.
- **Flag navegação simplificada** persistida: vai junto das preferências no backend SE o campo existir; senão fallback em SharedPreferences (chave `navegacao_simplificada`), reconciliado no ConfigController.carregar.
- **Verificação**: `flutter analyze` No issues + **62 testes verdes** + `flutter build web --release` OK (mobile e desktop).
- **Verificação visual** (harness estendido com fragments `config` e `contraste`): Configurações redesenhada confirmada (seções em cards com ícone + descrição sob cada toggle) e **alto contraste comprovadamente diferente** (bordas grossas pretas, texto preto puro, verde escuro, fonte pesada — resolve o "não vi diferença" do usuário). perfil-ong/home não capturaram no headless (dependem de fetch e não fecham no virtual-time-budget) → "Como chegar" confirmado por inspeção; home vista ao vivo. GOTCHA NOVO: headless no Windows precisa `--user-data-dir` ISOLADO por chamada (senão o lock de profile das janelas abertas faz o --screenshot não gravar nada silenciosamente).
- **Demo NO AR** p/ o usuário: API real 8080 (MySQL escola, migration bloqueio aplicada), mobile release 5100, desktop release 5200, 2 janelas Chrome abertas.
- Commits desta onda: mobile `571eb62` (WIP Mobile 2 integrado pelo Fable) — o Opus só corrigiu o import e NÃO precisou de commit de código novo (o fix entrou junto). Falta commitar: memórias + o import fix (se ficou fora). VERIFICAR git status ao fim.
**RESULTADO: os 13 tópicos da 2ª rodada estão TODOS entregues, testados e verificados. Roadmap segue 100%. Pendências humanas: pôster A0, ensaio, confirmar app no emulador Android (notebook). Deferred menor: utf8mb4, hospedar web, ongId no DTO de avaliação, coluna `estado` na entidade Ong (hoje desktop grava "Cidade - UF" no campo cidade).**

## 5ª RODADA (2026-07-06 noite tardia, Opus) — 9 tópicos, FECHAMENTO DO DIA
Usuário: "após fazer isso comite tudo pois não mudaria mais nada hoje." 3 agentes paralelos. Backend 115 testes (de2f5a0), mobile 62 (acc9c08), desktop 37 (520858a). Rebuild limpo + API reiniciada (de2f5a0 é só código, sem migration). 2 bugs de renderização exigiram PROVA por screenshot (feito).
- **Privacidade — ONG não via contato do doador**: o perfil do doador no DESKTOP não renderizava contato (só o mobile fora atualizado na 4ª rodada). Fix: model PerfilPublicoDoador ganhou email/telefone + seção "Contato" na tela desktop (só quando não-nulos). Backend de2f5a0 adicionou `ongId` que faltava em prestacoesRecebidas.
- **BUG da config em branco (desktop) — causa raiz elegante**: a barra "Salvar configurações" (bottomNavigationBar) usava `Center` SEM `heightFactor` → expandia até a altura da tela → Scaffold dava altura 0 ao corpo → só a barra aparecia. Disparava a cada mudança (mudança=barra aparece). Fix: `Center(heightFactor: 1.0)`. PROVADO com widget-test (ListView 720x0 → 720x771) + screenshots antes/depois.
- **FOGO finalmente visível (3ª tentativa)**: chip_foguinho refeito com fundo SÓLIDO (gradiente fogo→fogoEscuro) + chama BRANCA + texto BRANCO bold. VERIFICADO por screenshot (scratchpad/fogo.png): salta sobre card OURO/header verde/fundo claro. Lição: bug visual repetido = exigir prova por screenshot do agente.
- **Matches: Prestação/Avaliar só em Concluídas** (Ativas = só Conversar).
- **Feed Explorar auto-atualiza** (polling 8s / 30s se nav simplificada, refresh ao focar) — nova necessidade aparece sem refresh manual.
- **Ordenação do feed**: favoritas → urgentes → recentes → interesse em andamento (fim); dataCriacao desc dentro de cada grupo.
- **Re-interesse após conclusão** (backend de2f5a0): POST /interesses só bloqueia se há interesse EM ANDAMENTO (PENDENTE/ACEITO); após CONCLUIDO/RECUSADO permite novo. Mobile: "Interesse demonstrado" só enquanto em andamento; concluído → "Demonstrar interesse novamente".
- **Necessidades clicáveis no perfil da ONG** → abrem NecessidadeDetalheScreen (demonstrar interesse).
- **Prestações nos perfis: contraparte + dedupe + clique** (nos 2 apps): perfil do doador agrupa por ONG (clicável), perfil da ONG agrupa por doador "para <nome>" (clicável). Degrada se ids null.
FIM DO DIA 2026-07-06. 3 repos commitados/pushados. Migrations 2FA (4ª rodada) entram no MySQL da escola no próximo startup lá.

## 4ª RODADA DE FEEDBACK (2026-07-06 noite, Opus) — 10 tópicos do teste ao vivo, TODOS entregues
Usuário testou ao vivo (2 abas: doador João + ONG Lar Viva) e reportou 10 itens. 3 agentes paralelos (backend fdf9874 / mobile f7667dc / desktop 680a2a7). Backend 113 testes (era 97), mobile 62, desktop 31. Rebuild LIMPO dos 2 apps (evita registrant obsoleto). API 8080 reiniciada p/ aplicar migrations do 2FA. Smoke test dos endpoints novos na 8080 OK (login-2fa/alterar-email/editar-necessidade existem; login normal intacto). Demo no ar.
- **Tempo real (polling)**: painel ONG (aba Interesses) e Matches do doador agora se auto-atualizam (Timer 4s, 12s se nav simplificada; cancelado no dispose/fora da aba/background). ONG vê "Novo interesse recebido!" na hora; doador vê "Seu interesse foi aceito! 💚" e o match migra p/ Ativas com chat, sem sair e entrar. Decisão mantida: polling (não WebSocket).
- **Foguinho 🔥 legível**: chip redesenhado — fundo creme (0xFFFFF1E0) + fogo laranja + "N dias em 1º" bold + borda laranja. Contrasta sobre card OURO e nos 2 temas. (2ª tentativa; a 1ª só trocou p/ branco e sumia.)
- **Navegação simplificada REAL**: a persistência já estava certa; o bug era o carrossel não fazer setState ao mudar o flag → corrigido (listener _aoMudarConfig religa timer + setState) + aviso visível "Modo simplificado — deslize para ver mais" e carrossel estático. Confete/transições já respeitavam.
- **Privacidade real do DOADOR**: perfil-publico do doador passa a incluir email/telefone SÓ quando mostrarEmail/mostrarTelefone=true (omitidos via NON_NULL; sem preferência salva = oculto). Mobile mostra seção "Contato" quando vierem.
- **Concluída = chat vira "Histórico da conversa" (só leitura)** no desktop + botões limpos por estado (ACEITO: Conversar+Prestar contas+Doação recebida+Ver perfil; CONCLUIDO: Histórico+Prestar contas se pendente/senão Ver prestações+Avaliar+Ver perfil). Fluxo concluir→prestar em 10 dias intacto.
- **Agrupar por doador (desktop)**: doador com 2+ interesses vira ExpansionTile (nome+contador, concluídas no fim), 1 único "Ver perfil do doador" (corrige a recém-aceita sem o botão).
- **Alterar e-mail**: `PUT /usuarios/{id}/email {novoEmail,senha}` (401 senha errada, 409 duplicado); UI na seção Segurança dos 2 apps (atualiza a sessão).
- **2FA (verificação em duas etapas) — SIMULADA como o esqueci-senha** (decisão registrada; sem SMTP): campo `Preferencia.doisFatores` (int 0/1, default OFF → logins existentes intactos); login com 2FA on → 200 {requer2fa:true, email, codigoDemo} sem token → `POST /auth/login-2fa {email,codigo}` → tokens. UI: toggle nas Configurações + tela de código (card "Modo demonstração: XXXXXX") no login dos 2 apps. Tabela `dois_fatores_codigo` (Liquibase). Para virar e-mail real: plugar SMTP (mesma pegada do esqueci-senha). Usuário PODE pedir e-mail real se tiver conta SMTP.
- **Editar necessidade**: `PUT /necessidades/{id}` (só ONG dona, 403 senão) + PopupMenuButton Editar/Excluir na aba Necessidades do desktop (form reusado pré-preenchido).
- **Notificação de novo interesse**: já existia no InteresseService (dispara p/ a ONG); +teste provando.
Estado: 3 repos limpos/sincronizados (falta commitar esta memória). Migrations 2FA entram no MySQL da escola no próximo startup (já entraram na 8080 reiniciada).

## 3ª RODADA DE BUGS (2026-07-06 fim de tarde, Opus) — 4 bugs do usuário corrigidos
Após ver os apps, o usuário reportou 4 bugs:
1. **PDF não gera no perfil do doador** (mobile) e 2. **Fotos não funcionam em lugar nenhum** (`MissingPluginException: openFile file_selector` no desktop; image_picker idem no mobile). **CAUSA RAIZ ÚNICA (importante!)**: o `web_plugin_registrant.dart` gerado (em `.dart_tool`, gitignored) estava OBSOLETO — foi criado antes de image_picker/file_selector/printing serem adicionados, e `flutter build web` NÃO regenera o registrant sozinho (só `flutter pub get`/`flutter clean` regeneram). Então na web faltavam ImagePickerPlugin/FileSelectorWeb/PrintingPlugin. **FIX = `flutter clean` + `flutter pub get` + `flutter build web --release`** nos 2 apps → registrant regenerado com todos os plugins (confirmado: mobile agora tem ImagePickerPlugin+PrintingPlugin+UrlLauncher; desktop tem FileSelectorWeb+Printing). **NÃO é bug de código-fonte** — nos alvos reais (Android/mobile, Windows/desktop) as fotos SEMPRE funcionaram (plugins nativos). Só o preview web quebrava. **LIÇÃO p/ o notebook FECITEC: se foto/PDF falhar na web, rode `flutter clean` antes do build.** Nada a commitar por isso (artefato de build local).
3. **Perfil da ONG tinha 2 telas de edição** (a rica EditarOngScreen com capa/endereço/fotos/UF-Cidade E a antiga "Meu Perfil" simples com "URL da foto" e Estado texto-livre). Removida a antiga: deletado `lib/screens/ong/perfil_screen.dart` + botão "Meu Perfil" do painel + import (desktop commit `69a1d97`). Fica só a rica.
4. Rodar os apps de novo p/ o usuário ver → feito (5100/5200 com builds limpos + API 8080).
Estado: 3 repos limpos/sincronizados. Demo no ar. Os 4 bugs resolvidos e verificados (registrant conferido; tela duplicada sumiu).

## ESTADO FINAL DA SESSÃO FABLE (2026-07-06 ~13h) — HANDOFF PARA O OPUS (histórico)
O usuário trocou de modelo por limite de tokens. TUDO COMMITADO E PUSHADO nos 3 repos (árvores limpas). O que cada onda entregou:
- **BACKEND ✅ COMPLETO** (commit `cf9404a`, master): bloqueio (endpoints + enforcement completo, incl. /campanhas/destaques), privacidade real (campos REAIS: `mostrarEmail`/`mostrarTelefone` na entidade Preferencia; default de ONG sem registro = email OCULTO, telefone visível), dataCriacao (já existia + testes). **97 testes verdes** + 34/34 ao vivo. Migration `bloqueio` entra no próximo startup.
- **MOBILE 1 ✅ COMPLETO** (commits `a434507`+`4d02fab`+`d39f7bd`, main): foguinho branco, NecessidadeDetalheScreen (urgentes+feed abrem nela; ONG clicável), ONG destaque→perfil, "há X dias" no feed, "Interesse demonstrado"+reordenação, Impacto 4 destinos (stat novo "Doações PIX" teal→Minhas Doações), chip prestação nas Concluídas, cabeçalho Nova doação. 54 testes.
- **DESKTOP ✅ COMPLETO** (commits `7bf9b00`,`6db6191`,`01e0d84`,`f4be4b0`, main): bloquear/desbloquear no perfil do doador + tela Doadores bloqueados (Configurações>Privacidade), chat header clicável, fix Maps (launchUrl direto + fallback clipboard em utils/app_links.dart), Salvar configurações (barra fixa+PopScope), SeletorEstadoCidade com asset IBGE COMPLETO (5.571 municípios, assets/dados/municipios_por_uf.json 84KB). 27 testes. E2E do bloqueio validado contra a API 8080 real. GOTCHA: entidade Ong NÃO tem coluna `estado` → desktop grava "Cidade - UF" no campo `cidade` (parse `separarCidadeUf`).
- **MOBILE 2 ⚠️ ~95% PRONTO, INTEGRAÇÃO A REVISAR** (commit `571eb62`, main): os sub-agentes caíram por limite ANTES do relatório final, mas o código ficou: `flutter analyze` No issues + **62/62 testes** (incl. test/seletor_uf_cidade_test.dart novo). Eu (orquestrador) só corrigi 1 import faltante (mapEquals em configuracoes_screen.dart). Arquivos entregues: widgets/forms/seletor_uf_cidade.dart + asset IBGE aplicado em EditarPerfilScreen e cadastro_doador_page; configuracoes_screen redesenhada (cards+Salvar com detecção de mudança via mapEquals); config_controller + app_theme (alto contraste real + flag navegação simplificada); carrossel_campanhas + celebracao + page_transition (respeitam navegação simplificada); app_links/perfil_publico_ong_screen (fix Maps + "Como chegar" dir/?api=1); chat_screen (header clicável + 403/bloqueado); models interesse/perfil_publico_ong/preferencia (bloqueadoPelaOng/bloqueado/flags).

## O QUE FALTA (mandato para a sessão Opus — Onda 3)
1. **Revisar a integração do Mobile 2** (analyze/testes já passam, mas NÃO houve relatório final dos sub-agentes): conferir na prática as 6 tarefas — (a) UF antes de Cidade + autocomplete no editar perfil E no cadastro; (b) Salvar configurações só persiste no botão (Descartar reverte ConfigController); (c) alto contraste VISIVELMENTE diferente + navegação simplificada desliga carrossel automático/confete/animações; (d) fix Maps na web; (e) botão "Como chegar" (maps/dir sem origin); (f) chat header→perfil ONG, 403 desabilita envio, perfil {bloqueado:true}→tela mínima "Você não pode ver este perfil". Completar o que faltar; ver onde a flag navegação simplificada é persistida (backend não tem campo — provável SharedPreferences).
2. **`flutter build web --release`** no mobile (não foi rodado após a integração).
3. **Verificação visual por screenshots** das telas novas do mobile (harness versionado lib/main_screenshots.dart + tool/screenshots/; gotchas na seção do plano acima). Desktop já fez a sua.
4. **Rebuild dos 2 apps + subir demo** (API real 8080 MySQL escola + servidor.dart do scratchpad ou recriar, portas 5100/5200) p/ o usuário ver.
5. **Atualizar [[connect-ong-roadmap]]/[[connect-ong-deferred]]** com a rodada 2 concluída + commit final de memória. Marcar esta memória como encerrada.
NOTA: processos de fundo da sessão anterior (API 8080, servidores 5100/5200) foram DERRUBADOS quando a sessão caiu — subir de novo na demo. Autonomia total confirmada pelo usuário: não parar entre etapas.
